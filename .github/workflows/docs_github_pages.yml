name: Docs

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  docs:
    uses: hygeos/reusable-workflows/.github/workflows/docs_github_pages.yml@v1
    permissions:
      contents: write
